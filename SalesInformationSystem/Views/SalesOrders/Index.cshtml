@model IEnumerable<SalesInformationSystem.Models.SalesOrder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-md btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quotation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Invoice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quotation.QuotationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Invoice.InvoiceId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SalesOrderId" class="btn btn-md btn-success">Edit</a> 
                <a asp-action="Details" asp-route-id="@item.SalesOrderId" class="btn btn-md btn-primary">Details</a> 
                <a asp-action="Delete" asp-route-id="@item.SalesOrderId" class="btn btn-md btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
